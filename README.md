# ControlCabinetTrigger

To build with:

```shell
mvn clean package -U
```

To Start with

```shell
start.bat
```

or

```shell
java -DlogPath="." -Dlogback.configurationFile="logback.xml" -jar @project.name@.jar
```
